# PodFlow Environment Variables
# Copy this to .env and fill in your API keys.
# NEVER commit .env to version control.

# Required for Whisper API transcription
OPENAI_API_KEY=sk-...

# Required for AI metadata generation (if provider=anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# Required for S3-compatible hosting (Cloudflare R2, AWS S3, etc.)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
